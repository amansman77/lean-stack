{
  "name": "leanstack-bff",
  "version": "1.0.0",
  "description": "Lean Stack BFF API with Cloudflare Workers and Hono",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["cloudflare-workers", "hono", "supabase", "bff", "api"],
  "author": "Lean Stack Team",
  "license": "Apache-2.0",
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/node-server": "^1.0.0",
    "wrangler": "^3.0.0",
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
